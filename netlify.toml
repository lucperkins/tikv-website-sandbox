[build]
publish = "public"
command = "make build-production"

[build.environment]
HUGO_VERSION = "0.49"

[context.deploy-preview]
command = "make build-preview"

[context.branch-deploy]
command = "make build-preview"
